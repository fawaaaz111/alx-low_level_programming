#include "main.h"

/**
  * _abs - git the absolute value if a character
  * @c: input value
  *
  * Return: the absolute vale
  */

int _abs(int c);
{
	if (c >= 0)
		return (c);
	else
		return (c * -1);
}
